/** Default HTTP port for `apps/project-server` (local dev). */
export const DEFAULT_PROJECT_SERVER_PORT = 8787;

/** Default Vite port for `apps/editor`. */
export const DEFAULT_EDITOR_DEV_PORT = 5173;
