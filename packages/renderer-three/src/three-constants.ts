/** Default clear color when no project background is passed. */
export const DEFAULT_THREE_BACKGROUND = 0x1a1d27;

/** Editor orbit camera start position. */
export const EDITOR_CAMERA_POSITION = { x: 6, y: 5, z: 8 } as const;

export const EDITOR_CAMERA_FOV = 50;
export const EDITOR_CAMERA_NEAR = 0.1;
export const EDITOR_CAMERA_FAR = 2000;

/** Placeholder box half-extent when Model3D has no asset / not loaded. */
export const MODEL3D_PLACEHOLDER_HALF = 0.5;

/** Grid helper size for editor chrome. */
export const EDITOR_GRID_SIZE = 20;
export const EDITOR_GRID_DIVISIONS = 20;
