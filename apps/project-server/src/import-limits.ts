/** Max files accepted in one asset import multipart request. */
export const MAX_ASSET_IMPORT_FILES = 32;

/** Max bytes per uploaded file during asset import. */
export const MAX_ASSET_IMPORT_FILE_BYTES = 20 * 1024 * 1024;
